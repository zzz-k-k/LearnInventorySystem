# 架构与实现演进文档

## 当前项目基础

- Unity 版本：`6000.2.14f1`。
- 已安装可用包：`com.unity.ugui`、`com.unity.inputsystem`。
- 当前场景入口：`Assets/Scenes/SampleScene.unity`。
- 当前背包实现：已完成第一条 UI 骨架流程；此前提前生成的业务代码已删除，本次只新增三个 UI 职责脚本。
- 当前数据库实现：无。
- 已确认需求和用户流程已经保留；高层施工路线已确认，当前第一条 UI 骨架流程已经实现并验证。

## 用户提出的整体构建流程 v1

### 步骤 1：背包界面骨架与基础数据

- 创建一个可以打开的背包界面，界面中显示若干行列的方格。
- 右键物品时可以看到“拆分”“使用”等 UI 入口，但暂时不实现具体功能。
- 定义后续需要的数据类型，包括人物、人物背包和背包中的物体。

### 步骤 2：简单使用效果

- 编写物品使用代码。
- 第一版使用效果只需要修改人物的某些数字，以验证使用行为。

### 步骤 3：本地数据上的完整背包功能

- 数据暂时保存在本地。
- 实现拆分、移动、使用和丢弃，使背包主要功能能够正常运行。

### 步骤 4：接入数据库

- 接入数据库并编写数据库数据操作逻辑。
- 让移动、更新和删除等背包变化能够写入数据库。

## AI 对 v1 的需求覆盖评审

### 正确且有价值的部分

- 从可打开的界面骨架开始，能够较早看到成果。
- 先用简单数字变化验证使用效果，再完成全部交互，顺序合理。
- 先让功能在本地数据上运行，再接入数据库，可以缩小调试范围。

### v1 主要缺口

- 未明确交换、堆叠合并、搜索、丢弃确认和失败反馈。
- 未明确物品定义与玩家物品实例的区别。
- 数据库步骤缺少读取、新增、重新加载和失败验证。
- “本地数据”含义不明确。
- 步骤 3 同时包含多个主要行为，范围过大。

## 用户修正后的构建流程 v2

### 步骤 1：完整背包 UI 骨架与基础数据表达

准备完成：

- 创建一个能够打开的背包界面，并显示若干行列的方格。
- 提供拆分、使用、交换、堆叠合并、搜索和丢弃相关的 UI 入口或状态，但暂时不实现具体行为。
- 点击丢弃时能够看到确认与取消界面。
- 定义人物、人物背包、物品定义和玩家物品实例等后续需要的数据概念。

用户提出的可观察结果：

- 最终得到一个能够打开的背包界面和较完整的 UI 骨架。

### 步骤 2：简单使用效果

准备完成：

- 编写物品使用行为。
- 第一版效果只修改人物的某些数字。

用户提出的可观察结果：

- 打开背包后，物品能够被真正使用。

### 步骤 3：内存数据上的完整背包功能

准备完成：

- 数据只保存在当前运行内存中，不要求关闭程序后保留。
- 实现移动、交换、堆叠合并、拆分、搜索、使用和丢弃。

用户提出的可观察结果：

- 背包主要功能在一次运行期间可以正常使用。

### 步骤 4：接入 MySQL 并完成持久化

准备完成：

- 接入数据库并编写数据读取、新增、更新和删除逻辑。
- 让移动、交换、合并、拆分、使用和丢弃产生的数据变化能够保存。
- 非法操作需要取消，不产生错误数据。
- 增加重新加载验证。

用户提出的可观察结果：

- 背包连接数据库后能够完整正常使用，重新加载后仍能看到已保存结果。

## AI 对 v2 的复核

### 已补齐内容

- 已明确步骤 3 使用运行时内存，不是本地文件。
- 已加入交换、堆叠合并和搜索。
- 已加入丢弃确认与取消 UI。
- 已明确物品定义和玩家物品实例是两类数据。
- 已补充数据库读取、新增和重新加载验证。

### 进入对应流程后再明确（不阻塞总体路线）

1. 步骤 3 是高层阶段；进入该阶段时，再由用户选择其中一条可观察流程并决定后续顺序。
2. 步骤 1 的“完整 UI”仍较笼统：需要明确是否要显示示例物品、数量和空格状态，以及空背包时能看到什么。
3. 步骤 2 尚未明确修改人物的哪个数字、变化显示在哪里，以及使用成功后物品数量是否减少。
4. 搜索尚未明确是否同时覆盖名称、类型和标签，以及可观察结果是过滤还是高亮。
5. 丢弃尚未明确支持全部丢弃还是同时支持部分数量丢弃。
6. 步骤 4 只提到非法操作取消，尚未覆盖数据库连接失败、加载失败和保存失败时的可见反馈。
7. 尚未安排“本地 MySQL Demo 与生产服务器架构差异”的文档说明。

### 依赖与步骤大小评审

- 步骤 1 -> 步骤 2 -> 内存行为 -> 数据库持久化的总体顺序成立。
- 步骤 1 可以作为一条 UI 骨架流程，但必须给出更精确的完成画面。
- 步骤 2 与步骤 3 都包含“使用”，需要明确步骤 2 完成效果验证后，步骤 3 还要补充什么。
- 步骤 3 可以作为高层阶段保留；进入 Phase 4 后一次只选择其中一条流程实现。
- 步骤 4 可以保留为总体阶段，但实际构建时仍需拆成可独立验证的加载、保存和失败流程。

## 需求覆盖矩阵（按 v2）

| 需求/验收标准 | 对应构建步骤 | 覆盖状态 |
|---|---|---|
| 打开背包并显示物品、数量和空格 | 步骤 1 | 部分覆盖：UI 已提出，物品、数量和空状态画面仍需明确 |
| 空背包、连接失败和加载失败反馈 | 步骤 1、4 | 部分覆盖：空背包画面和数据库失败反馈未明确 |
| 移动到空格并保存 | 步骤 3、4 | 已覆盖方向，待拆分为可验证流程 |
| 不同物品交换并保存 | 步骤 1、3、4 | 已覆盖方向，步骤 1 只有 UI，功能在步骤 3、4 |
| 同种物品堆叠合并 | 步骤 1、3、4 | 已覆盖方向，待明确堆叠上限结果 |
| 拆分到空格且失败时不产生部分结果 | 步骤 1、3、4 | 部分覆盖：拆分和非法取消已提出，失败结果需在流程中明确 |
| 搜索名称、类型或标签 | 步骤 1、3 | 部分覆盖：搜索已提出，范围和显示结果未明确 |
| 使用物品并产生具体效果 | 步骤 2、3、4 | 部分覆盖：数字效果已提出，显示与物品数量变化未明确 |
| 确认后丢弃全部或部分物品 | 步骤 1、3、4 | 部分覆盖：确认取消已提出，部分数量丢弃未明确 |
| 数据库加载、保存和失败反馈 | 步骤 4 | 部分覆盖：CRUD 和重载已提出，连接、加载、保存失败反馈缺失 |
| 区分物品定义和玩家物品实例 | 步骤 1 | 已覆盖概念，待步骤 1 验证数据表达 |
| 说明本地 Demo 与生产架构差异 | 未安排 | 缺失 |

## 已确认施工路线

1. 完成可打开的背包 UI 骨架和基础数据表达。
2. 用简单人物数值变化验证物品使用。
3. 在运行时内存数据上完成背包主要行为。
4. 接入 MySQL，完成读取、保存和重新加载验证。

以上是高层阶段，不要求现在拆完所有内部流程。进入每个阶段时，由用户先选择并描述一条具体可观察流程，AI 评审通过后才实现。

全局风险类别由 `risk-radar.md` 维护。风险雷达不提前决定架构，只在每条流程完成后帮助 AI 检查是否存在此前未意识到的风险大类。

## 当前实际架构

当前只有背包的 View/UI 基础设施，还没有物品数据、背包规则、存储或数据库架构：

- `main.unity`：保存 Canvas、`InventoryWindow`、搜索区、格子容器和 `ContextPanel` 等固定 UI；Canvas 保持激活，窗口和菜单初始隐藏。
- `InventorySlot.prefab`：定义重复格子的 UI 外观并挂载 `InventorySlotView`；场景中的格子是该 Prefab 的实例。
- `InventoryWindowView`：挂在 Canvas 上，监听 B 键，只切换 `InventoryWindow`，并统一定位、显示和隐藏已有的 `ContextPanel`。
- `InventorySlotView`：接收所属格子的右键点击，从父级找到 `InventoryWindowView`，将点击位置转交给窗口视图。

当前调用方向为：`B 键 -> InventoryWindowView -> 背包窗口`，以及 `格子右键 -> InventorySlotView -> InventoryWindowView -> 右键菜单`。搜索与菜单命令尚未连接任何业务行为，因此当前还不是完整 MVC 系统。

## 架构演进记录

### 变化 1：清除提前设计与实现

- 原状态：文档提前规定完整分层、文件列表和数据库方案，项目中已经生成背包代码。
- 调整原因：改为由用户先提出覆盖全部需求的施工路线，再逐条构建可运行流程。
- 实际调整：删除 `Assets/InventoryDemo`；保留并清理需求与用户流程；架构回到空实现基线。
- 当前状态：施工路线 v2 已确认为高层路线，进入第一条 UI 骨架流程。

### 变化 2：用户补充施工路线 v2

- 补充内容：内存数据、交换、堆叠合并、搜索、丢弃确认、两类物品数据、数据库 CRUD 与重载。
- 当前结果：需求方向基本覆盖并确认为高层路线；内部顺序与行为细节延后到对应流程开始时确认。

### 变化 3：总体路线改为渐进式细化

- 原状态：要求在 Phase 3 立即拆分步骤 3 并回答搜索、丢弃、失败反馈等细节。
- 调整原因：总体路线只需要覆盖需求和依赖顺序，具体流程应在进入对应阶段时再细化。
- 实际调整：接受四个高层阶段；当前只细化第一条 UI 骨架流程。

### 变化 4：从用户行为流程生成最小 UI 技术骨架

- 用户给出的顺序：B 键打开表格；添加搜索区；右键格子显示三个选项；暂不实现业务功能。
- AI 补充：把行为翻译为画面创建、窗口状态、格子右键三个职责，并使用项目已有的 uGUI 与 Input System。
- 实际结果：新增三个 UI 脚本，完成结构编译和自动行为校验；没有提前加入数据类型、MVC Model、背包规则或数据库代码。

### 变化 5：固定 UI 从代码创建改为场景与 Prefab 编辑

- 触发证据：运行时代码包含全部布局细节，用户无法在 Unity 编辑器中直接调整固定窗口，后续图标、拖拽和布局修改会继续扩大 `InventoryUiBootstrap`。
- 用户方案：在 Unity 中搭建固定背包界面；窗口保存在 `main` 场景，重复格子使用 Prefab；`InventoryWindowView` 保留输入和交互职责，Bootstrap 删除。
- 规范化：Canvas 始终激活并承载控制器，只切换 `InventoryWindow`；右键复用场景中的 `ContextPanel`，不创建菜单或重复判断命中对象。
- 实际结果：`InventoryUiBootstrap` 已删除，场景引用和 Slot Prefab 组件已由 Unity API 保存，`main` 成为首个启动场景。搜索和菜单命令仍没有业务行为。
